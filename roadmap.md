## in the webapp:
- add two sliders for a reverb and delay in the browser synth 
  - with basic settings for both a general slider for amount, and a expand button allows for fine control. 

- the dimmed buttons are rendered too dark, it does not fit the brightness of the actual device. e.g. the dimmed middle character in the 3 digit display. 

- build a script that allows to autostrip / minimize the lua scripts


## in the serpentine dev script

death animation should be more alive, randomize the notesequence it plays





